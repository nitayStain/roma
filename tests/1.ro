int a = 150;
if(a >= 150) {
  string hello = "Hello, World";
}

int i = 0;
while(i < 67) {
  i++;
}

float meow = 150.5151;

// TODO: take care of uncallable expressions in the analyzer
what(hello(sub()()),2,3);

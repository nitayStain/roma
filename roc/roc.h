#ifndef ROC_H
#define ROC_H

// roc - the roma compiler. Umbrella header pulling in roc's whole public
// surface, for consumers (like the roma CLI driver) that just want "all
// of roc" rather than picking individual headers.
#include "options/options.h"
#include "lexer/lexer.h"
#include "parser/parser.h"
#include "error.h"
#include "logger.h"

#endif // ROC_H
